# FOR_Huang：这个"微博配图生成器"到底是怎么跑起来的

## 先说这东西是干什么的

你有一段微博文案，想配 9 张图，但不想一张张自己抠字排版。这个网页帮你把整个流程串起来：

**贴文案 → AI 把文案拆成 N 个"信息点" → 每个信息点自动排版成一张手机竖图 → 网页帮你把每张图"拍照"下载下来。**

听起来像是"调用一个 AI 服务 + 做点前端"，但里面藏着几个挺有意思的工程选择，值得记一下——尤其是那个差点让整个功能全废掉的 bug。

## 整体架构：一个没有后端的"客户端应用"

最关键的决定是：**这个网页没有后端服务器**。它就是一堆静态文件（HTML/CSS/JS），你双击打开或者用最简单的本地服务器跑起来就行，所有逻辑都在你的浏览器里跑完。

这不是偷懒，是刻意的取舍。想象一下另一种做法：搭一个 Node.js/Python 后端，后端存你的 OpenRouter key，后端调用 AI，后端用无头浏览器（headless Chrome）截图，再把图片传回给你。这个方案"更专业"，但代价是——你要租服务器、要维护它、要处理"服务器挂了怎么办"。对于一个你自己用的工具，这些代价完全不值得。

所以架构长这样：

```
你的浏览器
  ├─ 读/写 localStorage          存你的设置（key、模型、风格...）
  ├─ fetch() 直连 OpenRouter API   拆文案，AI 计算在 OpenRouter 那边
  ├─ html2canvas                  把网页上的卡片"拍"成图片
  └─ JSZip                        把多张图片打包成一个 zip
```

四个环节，没有一个需要你自己的服务器。这是"能不建后端就不建后端"思路的一个典型例子——**后端不是标配，是有明确需求（要瞒住密钥、要跑重计算、要多人共享状态）时才该加的成本**。这里三个需求都不存在：key 只你自己用、AI 计算在 OpenRouter 端完成、状态只有你一个人需要看到。

## 文件是怎么分的

```
info-card/
  index.html          页面骨架：设置区、文案输入区、卡片预览网格
  css/style.css        整体布局、按钮、输入框这些"通用件"
  css/templates.css     三套卡片风格的具体样式
  js/state.js          全局状态 + localStorage 存取
  js/openrouter.js      跟 OpenRouter 说话的逻辑
  js/render.js          把"状态"画成"卡片 DOM"
  js/export.js          把"卡片 DOM"拍成图片、打包下载
  js/app.js             按钮绑定、把上面几层串起来
```

这个拆法背后有个朴素但很好用的原则：**每个文件只负责一件事，数据单向流动**。流程是：

`state`（数据） → `render.js`（数据变成看得见的卡片） → `export.js`（看得见的卡片变成图片文件）

`app.js` 不自己画卡片、不自己拍照，它只是个"调度员"：用户点了按钮，它去问 `openrouter.js` 要数据，把数据塞进 `state`，喊 `render.js` 重画一遍。这样每一层出问题时，你一眼就知道去哪个文件找——UI 画错了看 `render.js`，AI 返回的东西不对看 `openrouter.js`，下载的图不对看 `export.js`。这比把所有逻辑都堆在一个大文件里好维护得多。

小说明：因为没用任何构建工具（Webpack/Vite 之类），JS 文件之间不是用 `import`/`export`，而是靠 `<script>` 标签按顺序加载，共享同一个全局作用域——这是最原始但对这种规模的项目最省心的做法，零配置、打开浏览器就能跑。

## 数据怎么"记住"你上次的选择

你提到"这个 H5 应该能记住我上次的选择"。这靠的是浏览器自带的 `localStorage`——一个每个网站专属的、断电也不会丢的小型键值存储。`state.js` 里做的事情很简单：

1. 页面打开时，`loadState()` 从 `localStorage` 里读一份 JSON，读不到就用默认值兜底；
2. 你改任何设置（模型、风格、比例……），`persist()` 就把当前状态整个存回去。

这个模式叫"状态持久化"，思路上跟你手机 App 记住"上次登录的账号""上次的深色模式设置"是一回事，只是浏览器版更简单——不需要账号体系，一个域名的存储就是你一个人的。

## AI 拆文案：让模型"听话"地按格式返回

这一步调用 OpenRouter 的 Chat Completions 接口（跟 OpenAI 的接口格式几乎一样，OpenRouter 本质是很多家模型的"统一转发站"）。这里有两个容易踩坑的地方，都处理了：

**第一，模型有时不听话，返回的信息点数量不对。** 比如你要 9 张图，它可能返回 8 个或 11 个。解决办法不是"报错完事"，而是自动重试一次：把"你上次返回了几个，数量不对，请务必返回正好 N 个"这句话加回去再问一次。这是个很实用的模式——**给模型一次"纠错"的机会，比一次不对就直接甩锅给用户要友好得多**。当然如果重试完还不对，就老实告诉你"返回了几个、要几个"，让你自己决定重试还是换模型，而不是悄悄丢数据或者硬凑数字。

**第二，模型有时不会乖乖只返回 JSON**，可能夹杂几句解释文字。所以解析逻辑是"先假设它规矩、直接 `JSON.parse`，失败了再用正则把文本里第一个 `{...}` 抠出来试一次"。这是一种很常见的"宽容式解析"思路：**先用最严格的方式尝试，失败了再退一步用更宽松的方式兜底**，而不是一上来就用最复杂的容错逻辑（过度设计），也不是一上来就假设输入永远合法（过于脆弱）。

## 卡片是怎么"长成"不同风格的

三套风格（简约大字、杂志排版、极简黑白）不是三份完全独立的代码，而是三个 CSS class（`tpl-bold`/`tpl-magazine`/`tpl-mono`），配色则是通过 CSS 自定义属性（`--bg-a`、`--accent` 等）从 JS 塞进每张卡片的 `style` 里。

这么做的好处是**风格（模板）和配色（主题色）是两个独立的维度，可以自由组合**，不用"简约大字风+珊瑚橘""简约大字风+薰衣草紫"……每种组合写一份 CSS。同理，图片比例（1:1、3:4、9:16 或你自己填的数字）也是通过 CSS 的 `aspect-ratio: var(--ar-w) / var(--ar-h)` 实现的，改比例只是改两个数字，卡片自动跟着变高变宽，完全不用重新排版。

卡片上的文字（关键词、补充说明）用的是 `contenteditable="true"`——直接让浏览器把这块区域变成"可编辑文本"，你点一下就能改字，不需要专门再做一套"编辑弹窗"。这是前端里一个挺讨巧的小技巧：**该用浏览器原生能力的地方就别自己造轮子**。

## 截图是怎么实现的：`html2canvas` 的魔法

这是最"黑科技"的一环。浏览器本身没有"把这个 div 拍成图片"的原生功能，所以借助了一个第三方库 `html2canvas`。它做的事情大致是：把 DOM 元素的每一层样式（背景、文字、边框、圆角……）重新在一个看不见的 `<canvas>` 画布上"画"一遍，画完了这个画布就能导出成 PNG。

为了让导出的图片够清晰（毕竟微博配图不能糊），代码里用了 `{ scale: 3 }` 这个参数——卡片在网页上显示可能只有 300 像素宽，但截图时会按 3 倍分辨率画，导出的图片实际有 900 像素宽，放大看也不糊。这跟你手机拍照时"高清模式"是类似的道理：显示可以小，但存下来的文件要经得起放大看。

打包下载用的是 `JSZip`：把每张卡片依次截图拿到图片数据，塞进一个 zip 对象，最后一次性生成 zip 文件触发下载。这样"下载全部"不会一次弹出 9 个下载框把你烦死，而是一个 zip 搞定。

## 踩到的一个大坑，以及它教会我的事

写完第一版代码后，我没有直接告诉你"好了"，而是先自己跑了一遍测试——用 Playwright（一个能操控真实浏览器的自动化工具）打开页面，模拟点击"拆分要点"。结果直接报错：

```
Failed to execute 'fetch' ... Failed to read the 'headers' property from 'RequestInit':
String contains non ISO-8859-1 code point.
```

翻译过来：**HTTP 请求头（header）的值不能包含中文字符，只能是拉丁字母/数字这类"ISO-8859-1"范围内的字符**。而我在代码里给 OpenRouter 请求加了一个 `X-Title: "微博配图生成器"` 的头（这是 OpenRouter 用来在他们后台统计"这个请求来自哪个应用"的可选字段），中文字符直接让浏览器的 `fetch` 函数在发出请求之前就报错崩溃。

这个 bug 如果没测试出来，后果是：**这个功能在任何人的浏览器里，一次都跑不起来**——不是偶尔失败，是必然失败，因为浏览器规范就是这么定的。改起来很简单（把这个头的值换成英文 `"Weibo Info Card Generator"`），但如果没有主动去跑一遍真实流程，这种"看起来代码没问题，但浏览器底层协议不允许"的坑，很容易漏到你手上才发现。

**这里的经验是：HTTP 头（无论是自己发请求还是写后端）要么用纯 ASCII/英文，要么做专门的编码转换（比如 URL 编码），这是浏览器和大多数 HTTP 库的硬规矩，不是哪个库的 bug，是标准本身的限制。** 以后凡是往 header 里塞"人类可读的说明文字"，下意识就该想到这一条。

也正因为这个坑，这个项目再次印证了一件事：**"写完了"和"跑通了"是两件事**。好的工程习惯不是把代码写出来就交付，而是至少拿真实环境跑一遍最关键的路径——哪怕没有真实的 OpenRouter key，也可以用测试工具"假装"OpenRouter 返回了一些数据（这个专业说法叫 mock，模拟），照样能验证"拆文案之后卡片能不能正常画出来、下载按钮能不能正常触发下载"。这次验证里，我确实这么做了：伪造了 OpenRouter 的返回结果，跑通了拆分 → 换风格 → 换配色 → 换比例 → 单张下载 → 打包下载的完整链路，并且真的把导出的 PNG 图片打开看了一眼，确认排版和文字都是对的，不是空白图片。

## 一些可以带走的经验

- **能不加后端就不加**：先问自己"这个需求真的需要服务器吗"，而不是默认就搭一套。密钥只你自己用、计算在第三方 API 完成、没有多人协作需求——这三条只要满足，纯前端往往就够了。
- **数据、渲染、导出分层**：`state` → `render` → `export` 这种单向流动的结构，调试时能快速定位问题出在哪一层，也方便以后加新风格、新导出格式而不用改动其他层。
- **给 AI 一次纠错机会，而不是一次不对就报错**：模型输出不总是完全服从指令，一次自动重试（带上"你刚才错在哪"的提示）往往比精心设计更复杂的提示词更管用。
- **HTTP header 只能用 ASCII**：这是浏览器/协议层面的硬限制，写中文说明文字时容易踩到，以后留意。
- **交付前至少跑一遍关键路径，哪怕要 mock 掉外部依赖**：这次如果不跑测试，中文请求头这个必然崩溃的 bug会直接交到你手上——"看起来没问题"和"真的没问题"之间，往往就差一次实际运行。
